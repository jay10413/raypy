[package]
name = "raypy"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
name = "raypy"

[dependencies]
pyo3 = { version = "0.21", features = ["extension-module"] }
rayon = "1.8"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
